{
  "name": "nightflare",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0",
  "description": "Nightscout-compatible CGM data platform running entirely on Cloudflare Workers (Workers + D1 + KV + Durable Objects), built around the real, unmodified Nightscout client bundle",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply nightflare-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply nightflare-db --remote",
    "build:client": "cd client && npm install && node prerender.js && npx webpack --mode production --config webpack/webpack.config.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "wrangler": "^4.20.0"
  },
  "dependencies": {
    "hono": "^4.6.9"
  }
}
